# Coalesced Memory Access（合并访存）

> **关联练习**：阶段二「矩阵转置」——用转置问题理解什么是合并访存、为什么 Naive 版慢、Shared Memory 版如何修复。

---

## 1. 概念：什么是合并访存？

GPU 的全局内存（Global Memory，即 HBM）带宽很高，但**延迟也很高**（数百个时钟周期）。为了尽量吃满带宽，硬件不会让每个线程单独发起一次内存事务，而是以 **Warp（32 个相邻线程）** 为单位，把它们的访存请求**合并（coalesce）**成尽可能少的内存事务。

**合并访存（Coalesced Memory Access）** 指的是：同一个 Warp 内的 32 个线程，访问的地址**连续、对齐**，硬件可以把它们打包成 **1～2 次** 128 字节左右的内存事务，一次性搬回数据。

反之，如果 32 个线程各自访问**相距很远**的地址（大 stride、随机索引），硬件需要发起**多次**事务，有效带宽大幅下降——这就是**非合并访存（uncoalesced / scattered access）**。

一句话总结：

> **相邻线程访问相邻地址 → 合并 → 高带宽；相邻线程访问分散地址 → 不合并 → 低带宽。**

> **注意Coalesced Memory Access只发生在Global Memory里面，就是你Wrap线程如果随机访问显存，那速度就慢，如果你访问的都是连续显存，那我一次性搬给你**

---

## 2. 硬件视角：Warp 如何访问 Global Memory

### 2.1 基本单位

| 概念 | 含义 |
|------|------|
| **Warp** | 32 个连续 `threadIdx` 的线程，同一时刻执行同一条指令 |
| **内存事务（Memory Transaction）** | SM 向 HBM 发起的一次读/写请求，通常以 32/64/128 字节为粒度 |
| **合并（Coalescing）** | 把 Warp 内多个线程的访存请求合并为少量事务 |

调度器按 Warp 调度，而不是按单个线程。因此**优化访存时，要站在「一个 Warp 的 32 个线程会访问哪些地址」的角度思考**，而不是只看某一个线程。

### 2.2 什么样的访问能合并？

以 `float`（4 字节）为例，一个 Warp 读 32 个连续 `float`，共 128 字节，通常只需 **1 次**事务。

```cpp
// 合并：lane i 访问 a[i]，地址连续
int i = blockIdx.x * blockDim.x + threadIdx.x;
float v = a[i];
```

```cpp
// 非合并：lane i 访问 a[i * 2]，相邻线程地址间隔 8 字节，但仍可能部分合并；
// 若 stride 更大（如按列访问行主序矩阵），则 32 个地址可能横跨很大范围 → 多次事务
float v = a[i * 2];
```

### 2.3 对齐要求

起始地址最好与访问粒度对齐（例如 128 字节边界）。未对齐时硬件仍可能合并，但可能多发起一次事务，浪费带宽。